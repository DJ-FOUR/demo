# AI 学研社 Android 项目完整开发方案

> 适用对象：初学者、vibe coding、Android Studio 项目
>
> 核心原则：先做能跑的第一版，再接云端，再做产品化。

## 1. 项目现状

当前项目已经有基础前端骨架，主要包括：

- `MainActivity.kt`：底部导航和页面切换
- `DashboardFragment.kt`：首页
- `CourseFragment.kt`：练习页
- `LabFragment.kt`：AI 实验室
- `GraphFragment.kt`：知识图谱
- `ProfileFragment.kt`：个人页
- `ChatBottomSheet.kt`：AI 对话面板
- 多个自定义 `View`：图谱、雷达图、进度仪表盘、手写板

说明：
- 现在不是从零开始。
- 当前最缺的是“数据层”和“业务层”，不是更多页面。

## 2. 总体路线

项目分三版走：

1. **第一版：纯本地 MVP**
   - 没有后端
   - 没有服务空间
   - 没有 API 调用
   - 用本地数据和模拟 AI，把产品跑通

2. **第二版：云端接入版**
   - 接入腾讯云服务空间
   - 用云函数、云数据库、认证、存储
   - App 通过接口拿真实数据

3. **最终版：产品化版本**
   - 完整账号体系
   - 云端同步
   - 真实 AI 能力
   - 上架和长期维护

## 3. 第一版做什么

第一版的目标是：做出一个**能安装、能打开、能操作、能演示**的 App。

### 3.1 功能范围
- 首页能显示问候语、待办、番茄钟
- 课程页能分步作答和提交
- 聊天页能发送消息并得到本地模拟回复
- 实验室能生成新题并显示状态
- 图谱页能正常展示学习状态
- 个人页能显示基础信息

### 3.2 第一版不做
- 不做登录注册
- 不做会员
- 不做腾讯云服务空间
- 不做真实 API
- 不做 DeepSeek / 大模型联网调用
- 不做多端同步
- 不做复杂后台管理

### 3.3 第一版怎么开发

推荐结构：

```text
UI(Fragment/View)
  -> ViewModel
  -> Repository 接口
  -> LocalRepository 实现
```

开发顺序：
1. 先把页面切换跑稳
2. 再把首页做成可操作
3. 再把课程页、聊天页、实验室做成闭环
4. 最后补图谱页和个人页

### 3.4 第一版数据策略
- 待办：内存列表，后续可存本地
- 聊天记录：内存列表
- 练习记录：页面临时状态
- 图谱和雷达图：固定样例数据
- AI 效果：本地模拟回复

### 3.5 第一版验收标准
- App 能正常启动
- 5 个底部 Tab 都能切换
- 主要按钮都有反馈
- 没有空白页
- 至少 1 个完整流程能闭环：输入 -> 处理 -> 返回结果

## 4. 第二版做什么

第二版的目标是：把“本地 App”升级成“有真实云端能力的 App”。

### 4.1 第二版新增能力
- 用户登录
- 数据云端保存
- 练习记录同步
- 聊天记录同步
- 题目生成接入真实 AI
- 学习画像和统计从云端读取

### 4.2 推荐云端架构

腾讯云服务空间建议承担这些角色：
- 云函数：业务入口
- 云数据库：保存用户、练习、聊天、统计数据
- 云存储：保存头像、附件、导出文件
- 云认证：账号登录和身份识别

建议通信方式：

```text
Android App -> HTTP/SDK -> 云函数 -> 云数据库 / AI 服务 -> 返回 JSON
```

### 4.3 第二版怎么开发

先做迁移，不要推翻第一版。

步骤：
1. 保留第一版的 `Repository` 接口
2. 新增 `RemoteRepository`
3. 把本地模拟数据替换为云端数据
4. 先接一个模块，比如聊天
5. 再接练习记录
6. 最后接统计和图谱

### 4.4 第二版重点
- 统一数据格式
- 统一错误处理
- 统一加载态
- 统一登录态
- 统一本地缓存和云端同步逻辑

### 4.5 第二版验收标准
- 能登录
- 能从云端读取和保存数据
- App 断网时有本地降级
- 重装或换设备后数据可恢复
- 真实 AI 能返回结果

## 5. 最终版做什么

最终版不是“功能越多越好”，而是“真正能长期使用”。

### 5.1 最终版目标
- 可稳定使用
- 可持续迭代
- 可上架发行
- 可支持学习场景的长期数据积累

### 5.2 最终版功能
- 账号系统完整
- 云端同步完整
- 学习画像可持续积累
- AI 题目生成真实可用
- AI 对话能记忆上下文
- 学习数据可导出
- 主题、设置、通知、反馈完整

### 5.3 最终版怎么开发

技术上要做三件事：
1. 业务逻辑彻底从 UI 里拆出去
2. 本地缓存和云端同步双轨运行
3. AI 能力走统一后端，不让 App 直接暴露密钥

### 5.4 最终版验收标准
- 稳定
- 可登录
- 可同步
- 可恢复
- 可打包上架
- 可长期维护

## 6. 推荐工程结构

建议按下面分包：

- `ui/`：页面
- `viewmodel/`：页面状态
- `model/`：数据模型
- `repository/`：统一数据接口
- `data/local/`：本地实现
- `data/remote/`：云端实现
- `service/`：模拟 AI 或真实 AI 调用封装
- `adapter/`：列表适配器
- `view/`：自定义 View

## 7. 核心模块拆解

### 7.1 首页
- 问候语
- 今日待办
- 番茄钟
- 今日掌握度
- 进入练习入口

### 7.2 课程页
- 题目展示
- 多步输入
- 提交
- AI 诊断提示
- 手写草稿区

### 7.3 聊天页
- 消息列表
- 输入框
- 快捷问题
- 本地模拟回复
- 未来接云端 AI

### 7.4 实验室
- 题目生成
- 加载态
- 内容态
- 空态
- 重新生成

### 7.5 图谱页
- 知识点图谱
- 雷达图
- 热力图
- 复习提醒

### 7.6 个人页
- 头像
- 昵称
- 邮箱
- 设置入口
- 关于页面

## 8. 开发节奏

建议按这个顺序：

1. 先让 App 稳定运行
2. 再把首页做完整
3. 再做课程闭环
4. 再做本地聊天
5. 再做实验室
6. 再做图谱和个人页
7. 再拆 `ViewModel` 和 `Repository`
8. 最后接腾讯云

## 9. 初学者的正确做法

你现在最需要的不是“把所有技术一次学完”，而是：

- 每次只做一个模块
- 每次只改少量文件
- 每次都先跑通再继续
- 先假数据，后真数据
- 先本地，后云端

## 10. 给 DeepSeek 的执行要求

让 DeepSeek 改代码时，必须遵守：

1. 先列出要改的文件
2. 一次只改一个功能
3. 不要提前写云端
4. 不要把逻辑继续堆在 Fragment
5. 优先补 `ViewModel` 和 `Repository`
6. 改完后保证能编译
7. 如需接云端，只新增实现类，不推翻现有结构

## 11. 总结

这套方案不是一条“先后端后前端”的路，而是一条更适合初学者的路：

- 第一版：做出本地可运行 MVP
- 第二版：接入腾讯云，做云端同步和真实 AI
- 最终版：做成可长期使用、可上架的产品

所以你的全局思路可以记成一句话：

**先把前端和本地闭环做出来，再把云端能力一层层加上去。**
